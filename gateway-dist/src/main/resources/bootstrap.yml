spring:
  application:
    name: gateway-dist
  profiles:
    active: dev
  cloud:
    kubernetes:
      reload:
        enabled: true

# 要使用 spring.cloud.kubernetes.reload.enabled 默认就必须同时开始actuator的restart endpoint
management:
  endpoint:
    restart:
      enabled: true